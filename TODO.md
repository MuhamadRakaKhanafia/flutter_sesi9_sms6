# TODO
- [ ] Inspect and understand `lib/pages/homepage.dart` compile errors.
- [ ] Fix broken import for `UserListPage` in `lib/pages/homepage.dart`.
- [ ] Remove duplicate `color:` argument in `BoxShadow` in `lib/pages/homepage.dart`.
- [ ] Fix remaining syntax issues in `lib/pages/homepage.dart` (near end of file) so it compiles.
- [ ] Run `flutter analyze` / `flutter test` to verify no remaining errors.

